# Общий файл переменных окружения для backend и frontend
# Скопируйте этот файл в .env и заполните значения

# ============================================
# Backend переменные (runtime)
# ============================================
PORT=8080
DB_PATH=./data/app.db
JWT_SECRET=your-super-secret-key-change-in-production-min-32-chars
JWT_EXPIRY=24h
CORS_ORIGIN=*
ENVIRONMENT=development

# ============================================
# Frontend переменные (build time - для Vite)
# ============================================
# Эти переменные используются только во время сборки (build time)
# Для локальной разработки с Docker:
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws

# Для production на сервере (если frontend и backend на одном домене через nginx):
# VITE_API_URL=/api
# VITE_WS_URL=wss://your-domain.com/ws

# Для production на сервере (если frontend и backend на разных доменах):
# VITE_API_URL=https://api.your-domain.com/api
# VITE_WS_URL=wss://api.your-domain.com/ws

# ============================================
# Docker Compose переменные
# ============================================
# Эти переменные используются docker-compose.yml
COMPOSE_PROJECT_NAME=bbp
